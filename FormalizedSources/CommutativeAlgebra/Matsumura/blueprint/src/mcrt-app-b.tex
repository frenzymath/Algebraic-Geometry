





\begin{theorem}[Printed Theorem B2]\label{mcrt-auto-theorem-b2}\dcref{B2}\source{matsumura-commutative-ring-theory:page-0291}\uses{}
\begin{verbatim}
Suppose that the double complex K? satisfies
          KP4=0             for p or q-c0
and
          Hq(KP?) = 0               for q > 0 and all p.
Then making Xp = Ker(d?:KP,?                 -+   KP7?) into a complex   X? by means
of d?, we have
             H?(K)   z H?(X?),
If in addition       HP(fCg) = 0 for p > 0 and all CJthen
             H?( Y?) N H?(K)     N H?(X?),
where Y? is the complex made from Yg = Ker(d?:Kosg --+K?,g).
\end{verbatim}
\end{theorem}
\begin{proof}
\notready
\end{proof}


\begin{theorem}[Printed Theorem B3]\label{mcrt-auto-theorem-b3}\dcref{B3}\source{matsumura-commutative-ring-theory:page-0292}\uses{}
\begin{verbatim}
A necessary and sufficient condition for an A-module
I to be injective is that for any ideal a of A, and any mapcp:a -Z,      it is
possible to extend rp to a map from the whole of A to 1.
\end{verbatim}
\end{theorem}
\begin{proof}
\notready
\end{proof}


\begin{theorem}[Printed Theorem B4]\label{mcrt-auto-theorem-b4}\dcref{B4}\source{matsumura-commutative-ring-theory:page-0296}\uses{}
\begin{verbatim}
An A-module A4 is injective if and only if it has no essential
extensions except M itself.
\end{verbatim}
\end{theorem}
\begin{proof}
\notready
\end{proof}
