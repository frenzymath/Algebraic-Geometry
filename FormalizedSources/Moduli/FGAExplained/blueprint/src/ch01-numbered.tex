% Source-backed numbered records rebuilt from the visual page ledger.
% Statements and source proofs are kept in separate environments.

\begin{remark}[Source statement 1.5]\label{fga-rem-1-5}\dcref{1.5}\source{fga-explained:page-0018}\uses{}
\begin{verbatim}
It is not enough to suppose that there is a coveringof Y by
open quasi-compact subschemes V^,such that the inverseimage of each Vi isquasi-
compact in X, without additionalhypotheses.For example, considera ring A that
does not satisfythe ascending chain conditionon radicalideals(forexample, a
polynomial ring in infinitely
                            many variables),and setX ? Spec^. In X therewill
be an open subset U that isnot quasi-compact;denote by Y the scheme obtained
by gluingtwo copiesof X togetheralong [/,and by /: X ^ Y"the inclusionof one
of the copies.Then Y and X are both quasi-compact;on the other hand there is
an affineopen subsetof Y (theother copy of X) whose inverseimage in X is[/,so
/ isnot quasi-compact.
\end{verbatim}
\end{remark}
\begin{proof}
\notready The source states this result without supplying a proof at this location.
\end{proof}


\begin{definition}[Source statement 1.10]\label{fga-def-1-10}\dcref{1.10}\source{fga-explained:page-0019}\uses{}
\begin{verbatim}
A morphism of schemes /: X ^ Y" isfaithfully
                                                               flatifitis
flatand surjective.
    Let B be an algebraover A. We say that B isfaithfully
                                                        flatifthe associated
morphism of schemes Spec 5 -^ Spec^ isfaithfully
                                               flat.
\end{verbatim}
\end{definition}
\begin{proof}
\notready The source states this result without supplying a proof at this location.
\end{proof}


\begin{remark}[Source statement 1.14]\label{fga-rem-1-14}\dcref{1.14}\source{fga-explained:page-0019}\uses{}
\begin{verbatim}
For thiswe need to assume that / is quasi-compact,itisnot
                                   flat.For example, letY be an integralsmooth
enough to assume that itisfaithfully
curve over an algebraically
                          closedfield,X the disjointunion of the Spec Oy^y over
allclosedpointsy eY. The naturalprojection/: X ^ Y" isclearlyflat.However,
if5 isa subsetof Y containingthe genericpoint,then f~^S isalways open in X,
while S isopen in Y ifand only ifitscomplement isfinite.
\end{verbatim}
\end{remark}
\begin{proof}
\notready The source states this result without supplying a proof at this location.
\end{proof}
