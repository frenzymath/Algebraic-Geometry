import StacksPart07Lib.RepresentableMorphisms

/-!
# Representability

Canonical entry point for the relative-representability API used throughout
the Part 07 formalization.  The implementation lives in
`RepresentableMorphisms`; this forwarding module keeps imports stable while
the later chapters add stack-specific constructions.
-/
